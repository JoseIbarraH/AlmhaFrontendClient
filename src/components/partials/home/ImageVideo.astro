---
const { image } = Astro.props;
import { getRelativeLocaleUrl } from "astro:i18n";
import Button from "@/components/ui/Button.astro";

const currentLang = Astro.currentLocale || "es";

console.log("imagen: ", image);
---

<div class="relative w-full h-screen">
  {
    image && image.length > 0 && (
      <>
        <div class="absolute inset-0">
          <img src={image[0].image} alt="" class="w-full h-full object-cover" />
          <div class="absolute inset-0 bg-[#513A31]/40" />
        </div>

        <div class="relative z-20 flex h-full flex-col items-center justify-center px-6 text-center">
          <div id="content-container">
            <p class="mb-6 text-xs font-light tracking-[0.4em] uppercase text-[#FEEBDD]">
              Turismo Estético Premium
            </p>

            <h1 class="max-w-4xl font-serif text-4xl font-bold leading-tight text-[#FFF9F6] md:text-6xl lg:text-7xl whitespace-pre-line text-balance">
              {image[0].title}
            </h1>

            <p class="mt-6 max-w-xl text-base font-light leading-relaxed text-[#FEEBDD]/90 md:text-lg">
              {image[0].subtitle}
            </p>

            <div class="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">
              <Button href={getRelativeLocaleUrl(currentLang, "procedure")}>
                Ver Planes
              </Button>
              <Button
                href={getRelativeLocaleUrl(currentLang, "about")}
                variant="secondary"
              >
                Conocer Más
              </Button>
            </div>
          </div>
        </div>
      </>
    )
  }
</div>
